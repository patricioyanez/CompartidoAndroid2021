
public abstract class ClaseAbstracta implements IOperaciones{
// tiene al menos 1 método abstracto -> solo se define y no tienen { codigo }
// no se puede instanciar, pero se pueden ocupar como variables.
// se definen principalmente para ser heredadas.	
	
	public abstract int acelerar(int segundos, int kms);
}
